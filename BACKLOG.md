# Backlog

## Features

- [ ] Add `-acks` flag to allow `acks=all` with idempotent producer (currently hardcoded to `acks=1` for speed; `acks=all` would enable exactly-once semantics for testing delivery guarantees)

## Improvements

- [ ] Support both Docker and Podman in Makefile and README
